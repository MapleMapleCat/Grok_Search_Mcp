# grok-mcp 运行配置示例。复制为 .env 并填入真实值：
#   cp .env.example .env   （Windows PowerShell：Copy-Item .env.example .env）
# 注意：.env 已在 .gitignore / .dockerignore 中排除，真实凭证不会进入版本库或镜像。

# ===== 上游 CPA 配置（CPA_API_KEY 必填）=====
CPA_BASE_URL=http://host.docker.internal:8317
CPA_API_KEY=replace-with-your-cpa-api-key

# ===== grok-mcp HTTP 服务 =====
GROK_HTTP_ADDR=:8080
GROK_DB_PATH=/app/data/grok-mcp.db

# ===== 面板 API（/panel/v1，必填）=====
# 注册/登录无需鉴权；其他面板接口需 Authorization: Bearer <JWT>。
GROK_JWT_SECRET=replace-with-a-strong-random-jwt-secret

# 新注册用户默认额度（0 表示不限总次数/成功次数）
GROK_DEFAULT_USER_RPM=60
GROK_DEFAULT_USER_TOTAL_LIMIT=0
GROK_DEFAULT_USER_SUCCESS_LIMIT=0

# ===== 可选 =====
GROK_MODEL=grok-4.3
GROK_HTTP_TIMEOUT=120
# GROK_MCP_DEBUG=1